# Local .terraform directories
**/.terraform/*

# .tfstate files
# These contain sensitive data and the current state of your infrastructure.
# They should NEVER be committed to version control.
*.tfstate
*.tfstate.*

# Crash log files
crash.log
crash.*.log

# Exclude all .tfvars files, which are likely to contain sensitive data,
# such as password, private keys, and other secrets.
# You might want to allow `terraform.tfvars` if it contains only non-sensitive defaults,
# but it is safer to rely on *.auto.tfvars for local development secrets.
*.tfvars
*.tfvars.json

# Ignore override files as they are usually used to override resources locally and so
# are not checked in
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Ignore CLI configuration files
.terraformrc
terraform.rc

# Ignore plan files (generated by terraform plan -out=...)
*.tfplan

# Terragrunt cache (if using Terragrunt)
.terragrunt-cache/

# --- OS and IDE specifics ---

# macOS
.DS_Store

# Windows
Thumbs.db

# VS Code
.vscode/

# IntelliJ / JetBrains
.idea/